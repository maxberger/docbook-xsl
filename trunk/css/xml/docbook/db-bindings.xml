<?xml version="1.0"?>
<bindings xmlns="http://www.mozilla.org/xbl"
     xmlns:html="http://www.w3.org/1999/xhtml">
  <binding id="ulink">
    <handlers>
      <handler event="click" button="0" action="window.location=this.attributes.url.value;"/>
    </handler>
  </binding>

  <binding id="image">
    <content>
      <html:img/>
    </content>

    <implementation>
      <constructor>
        //alert(this.getAttribute("fileref"));
        //alert(document.getAnonymousNodes(this)[0]);
	var img = document.getAnonymousNodes(this)[0];
	var file = this.getAttribute("fileref");
	var f = function () {
	  img.src = file;
	}

	setTimeout(f, 0);
      </constructor>
    </implementation>
  </binding>

</bindings>
